enum ViewState { idle, refresh, loading, complete, error }

enum SlideDirection { left, right, up }

enum ConnectivityStatus { online, offline }

enum DemoType { image, input, button, box }

enum PositionImageBoxType { top, bottom }

enum BoxCustomType { imageCard }

enum ButtonType { normal, iconButton }

enum ImageLoaderType { user, image }

enum LoadMoreType { isLoading, idle }

enum TripListType { normal, history }

enum ItemStudentType { normal, scanQR }

enum ConfirmAttendanceType { normal, scanQR }
